---
description: 
globs:
alwaysApply: true
---

# CloudCVM.com 项目规则文档

> **版本**: 5.0.0
> **更新日期**: 2026-01-02
> **文档状态**: Active

## 🎯 项目概述

CloudCVM.com 是一个基于 **Next.js 15** (App Router) + **React 19** + **TypeScript** 构建的现代化云服务商官网。项目集成了高性能的 **Tailwind CSS 4** 样式系统、**Three.js** 3D 渲染以及 **Framer Motion** 动画库，旨在提供流畅的用户体验和丰富的视觉效果。

---

## 🏗️ 技术栈规范

### 核心框架与库

| 类别 | 技术 | 版本 | 说明 |
| :--- | :--- | :--- | :--- |
| **框架** | Next.js | ^15.0.0 | 使用 App Router 架构 |
| **UI 库** | React | ^19.0.0 | 利用 React Server Components |
| **语言** | TypeScript | ^5.8.3 | 强类型开发 |
| **样式** | Tailwind CSS | ^4.1.11 | CSS-first 配置 (@theme) |
| **动画** | Framer Motion | ^12.23.12 | 页面交互动画 |
| **3D 渲染** | Three.js | ^0.179.1 | 3D 场景展示 |
| **图标** | Heroicons / Lucide | Latest | UI 图标库 |

### 开发工具

*   **包管理器**: npm (基于 `package-lock.json`)
*   **代码规范**: ESLint (Next.js config) + Prettier
*   **格式化配置**:
    *   Single Quote: `true`
    *   Semi: `false`
    *   Tailwind Plugin: Enabled

---

## 📁 目录结构规范

项目遵循 Next.js App Router 的标准结构，源码位于 `src` 目录下。

```
e:\Github\cloudcvm.com\
├── .next/                  # 构建输出目录
├── .trae/                  # Trae IDE 配置
├── out/                    # 静态导出目录 (SSG)
├── public/                 # 静态资源 (图片, robots.txt 等)
├── scripts/                # 构建辅助脚本 (如 SEO 验证)
├── src/                    # 源代码根目录
│   ├── app/                # App Router 路由定义
│   │   ├── (feature)/      # 功能模块路由 (如 agent, cdn 等)
│   │   ├── layout.tsx      # 全局根布局 (RootLayout)
│   │   ├── page.tsx        # 首页入口
│   │   └── globals.css     # (可选) 全局样式
│   ├── components/         # 组件库
│   │   ├── ai/             # AI 相关业务组件
│   │   ├── carousel/       # 轮播图组件
│   │   ├── common/         # 通用基础组件 (Button, Container 等)
│   │   ├── css/            # CSS Modules (特定组件样式)
│   │   └── [Feature].tsx   # 页面级功能组件
│   └── styles/             # 样式配置目录
│       └── tailwind.css    # Tailwind 4 入口与主题配置
├── next.config.js          # Next.js 配置文件
├── package.json            # 项目依赖与脚本
├── tailwind.config.js      # (移除) 迁移至 CSS 变量配置
└── tsconfig.json           # TypeScript 配置
```

---

## 📝 命名与代码规范

### 文件命名

*   **组件文件**: 使用 `PascalCase` (大驼峰)，如 `HeaderCarousel.tsx`, `Hero.tsx`。
*   **页面文件**: Next.js 路由文件统一为 `page.tsx`, `layout.tsx`, `loading.tsx` 等。
*   **工具/配置**: 使用 `camelCase` 或 `kebab-case`，如 `validate-seo.js`, `tailwind.css`。
*   **CSS Modules**: `[ComponentName].module.css`。

### 代码风格

*   **React 组件**: 优先使用函数式组件 (`function ComponentName() {}`) 和 Hooks。
*   **类型定义**:
    *   使用 `interface` 定义 Props 和对象结构。
    *   避免使用 `any`，尽量完善类型定义。
*   **类名合并**: 使用 `clsx` 或 `tailwind-merge` 处理条件类名。
    ```tsx
    import clsx from 'clsx'
    <div className={clsx('base-class', condition && 'active-class')} />
    ```

### 导入顺序

1.  React / Next.js 内置模块
2.  第三方依赖 (clsx, framer-motion 等)
3.  本地绝对路径导入 (`@/components/...`, `@/styles/...`)
4.  本地相对路径导入 (`./...`)
5.  样式文件导入

---

## 🎨 样式系统 (Tailwind CSS 4)

项目采用 Tailwind CSS v4，主题配置直接在 `src/styles/tailwind.css` 中通过 CSS 变量定义。

### 主题配置 (@theme)

*   **颜色系统**:
    *   品牌主色 (Brand Blue): 统一使用 `#3860F4`（hover 为 `#2b4dd4`）。
    *   为了全站一致性：在 `src/styles/tailwind.css` 中把 Tailwind 的 `--color-blue-600/700` 映射为品牌蓝，避免 `bg-blue-600` 与硬编码品牌蓝产生色差。
    *   变量示例: `--color-brand: #3860F4` / `--color-brand-hover: #2b4dd4`
*   **字体系统**:
    *   Sans: `Inter` (变量 `--font-inter`)
    *   Display: `Lexend` (变量 `--font-lexend`)
*   **排版**:
    *   自定义了从 `text-xs` 到 `text-9xl` 的字号与行高。
*   **容器**:
    *   `--container-2xl: 80rem`

### 样式编写指南

1.  **优先使用 Utility Classes**: 直接在 JSX 中编写 Tailwind 类名。
2.  **CSS Variables**: 对于动态值或主题配置，使用 CSS 变量。
3.  **Component Classes**: 对于高复用的复杂组件样式，在 `tailwind.css` 的 `@layer components` 中定义 BEM 风格的类 (如 `.feature-card`)，并在组件中使用。
4.  **CSS Modules**: 仅在需要复杂动画或无法用 Tailwind 简单实现的场景下使用 CSS Modules (位于 `src/components/css/`)。
5.  **滚动条隐藏**: 使用 `.scrollbar-hide` 类 (定义在 `tailwind.css`)。

---

## 🧩 组件开发指南

### 目录归属

*   **通用组件**: 放入 `src/components/common/` (如 `Button.tsx`, `Container.tsx`)。这些组件应高度可复用，不包含具体业务逻辑。
*   **业务组件**: 放入 `src/components/[feature]/` (如 `ai/`, `carousel/`)。这些组件与特定业务功能绑定。

### 服务端 vs 客户端组件

*   **默认服务端**: Next.js App Router 默认为 Server Components。
*   **客户端组件**: 当需要使用 Hooks (`useState`, `useEffect`) 或事件监听时，必须在文件顶部添加 `'use client'` 指令。

---

## 🚀 构建与部署

*   **开发环境**: `npm run dev`
*   **生产构建**: `npm run build`
*   **静态导出**: 项目包含 `out` 目录，可能使用了 `output: 'export'` 配置 (需确认 `next.config.js`)，适用于静态站点托管。
*   **SEO 检查**: `npm run validate-seo` 用于验证 SEO 配置。