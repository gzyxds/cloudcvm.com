# 代码规范与优化 · 分步执行记录

> **定位**：本文是 **S1~S10 / P1~P3 的会话执行日志**（改一步→验证→再继续）。
> 规范正文与验收清单在 `代码规范与优化方案.md`；架构事实与踩坑在 `项目长期记忆.md`。
> 记录中的文档名 `CODE_STANDARDS_AND_OPTIMIZATION.md` 即现在的 `代码规范与优化方案.md`
> （已移入 `开发文档/` 目录，文件名统一）。
>
> **阅读提示**：章节按当时执行顺序记录，编号不完全连续——`S10` 段落在 `S8` 之前、
> 「更正/重要纠正」插在阶段之间，均为当时的实际执行顺序，保留原貌未重排。

# 2026-09-21

## CloudCVM 代码规范与优化（承接 2026-09-18）

### 已完成（S1 阶段）

- 产出《代码规范与优化方案》（13 章，含 §13 执行记录）
- 删除 8 个零引用文件：`common/QRCode.tsx`(585)、`Pricing.tsx`(171)、`Fields.tsx`(47)、
  `ai/BananaProductData.ts`(64)、`NavLink.tsx`(18)、`SlimLayout.tsx`(9)、
  `carousel/index.ts`(7)、`config/index.ts`(57) —— 共 958 行
- 新增 `src/hooks/useActiveSection.ts`，替换 12 份重复 scroll-spy（净减 362 行）
  - 附带修复：依赖从 `sectionIds`（每次渲染新数组）改为 `sectionIds.join('|')`，
    解决 IntersectionObserver 反复重建
- 新增 `src/components/layout/SiteShell.tsx`，统一 14 份重复 layout（净减 135 行）
  - retail 的 `bg-white` 通过 className prop 保留；21 份 SEO metadata 全部保留
- `/human` 的 PixelBlast（three+postprocessing）改 `dynamic({ssr:false})`
- 视频 `preload="auto"` → `"metadata"`（Faqs / about / CatSections）
- `tsconfig.json` include/exclude 收窄；新增 `.editorconfig` / `.gitattributes`
- `package.json` 补 `typecheck` / `format` / `images:optimize`

**结果**：git 43 files changed, +75 / −1592；src 48,859 → 47,549 行。
验证：`tsc --noEmit` 0 error；`npm run build` 53/53 静态页。

### 修复：postcss `url()` 告警（我自己的文档引发的）

- 根因：《代码规范与优化方案》（时名 `CODE_STANDARDS_AND_OPTIMIZATION.md`）里写了示例 `bg-[url()]`，
  **Tailwind v4 默认扫描根目录所有文件含 .md**，把文档示例当真实类名提取
- 修复：改文档措辞 + `src/styles/tailwind.css` 加
  `@source not "../../**/*.md";` 和 `@source not "../../scripts";`
- 验证：对照组（`src/_probe.tsx` 命中 / 根 `_probe.md` 不命中）证明规则精确生效
- 产物 CSS 347KB → 271KB（−22%）

### S2 阶段（同日，用户要求「改一步→验证→无错再继续」）

- `prettier.config.js` 补 `printWidth:100` / `endOfLine:'lf'`（实测 p90 行宽 77、p99 178）
- 新增 `src/app/error.tsx`(66 行，自带 Header/Footer) 与 `src/app/loading.tsx`(33 行骨架，不含 Header/Footer)
  - ⚠️ **后记（2026-09-21 晚）**：`loading.tsx` 已在静态导出排查中**整体删除**——静态导出会把它的骨架烘焙进 HTML 首屏（真实内容藏于 `<div hidden id="S:0">`，hydration 后才替换），导致每次刷新闪现"两个小圆角块"。详见《代码规范与优化方案》§13.16
- **删除 `src/components/ai/Hero.tsx`（324 行，零引用）**
  - 教训：首轮死代码扫描按「文件 stem」匹配，`@/components/Hero` 的导入让 `ai/Hero` 误判为已引用。
    **扫描死代码必须用完整导入路径匹配，否则同名文件会漏判。**
- 命名整改 9 项（每步 tsc 验证）：
  `common/Top`→`FloatingToolbar`、`common/Erlie`→`TwoColumnShowcase`、
  `common/Accordion`→`CapabilityGrid`、`common/ecshop`→`EcommerceProducts`(导出 ProductsSection 同步改，消除与 ai/ProductsSection 同名冲突)、
  `common/Cardprice`→`PriceCard`、`ai/Aisd`→`AiSolutionSection`、`ai/AIscene`→`AiScene`(跨 6 文件)、
  `Logoclouds`→`LogoClouds`(含 CSS Module)、`ai/Aicarousel`→`AiCarousel`
  - **资源文件名不可随组件改名**：`/images/Logoclouds/*`(14 处)、`/images/background/Cardprice.webp` 必须原样保留

**累计**：src 48,859 → 47,313 行（−1,546），文件 137 → 132。
终验：`tsc` 0 error；`npm run build` ✓Compiled / ✓53/53 / First Load JS 100 kB；旧命名零残留。

### S3 阶段（同日）

- 新建 `src/types/product.ts`：商品 `Product` 接口 3 处定义 → 1 处
  - `ServiceTabs.tsx` 的同名 `Product` 实为**服务卡片**（id/name/tags），只改名 `ServiceProduct`，**不合并**
- 新建 `src/lib/format.ts`：`formatPrice`（整数千分位）/ `formatPriceWithDecimals`（两位小数）
  - 7 处内联格式化 → 1 处；刻意保留两种输出差异，未擅自改线上价格展示
  - 顺带修掉无参 `toLocaleString()` 的 locale 不确定性（固定 zh-CN，避免 hydration 不匹配）
- 目录迁移 11 个文件：`ui/`(Button,Container,Logo)、`layout/`(SiteShell,Header,Footer,MegaMenu,
  MobileMenu,FloatingToolbar,navStyles)、`effects/`(PixelBlast)、`analytics/`(Analytics)
  - 82 个文件导入重写；**遗漏 3 处相对导入 `./Container`（Faqs/LogoClouds/Zone），被 tsc 捕获后修正**
  - 教训：批量路径迁移必须同时处理相对导入，不能只改 `@/` 别名

**累计**：src 48,859 → 47,343 行（−1,516），文件 137 → 134。
终验：`tsc` 0 error；`npm run build` ✓Compiled / ✓53/53。

### S4 阶段（同日）：sections/ 归位

- 用脚本统计每个区块被哪些页面引用后再分类（不是拍脑袋）：
  `sections/home/`(9，仅首页) / `sections/shared/`(13，≥2 页) / `sections/ai/`(10+data)
- `components/` 根目录散落文件 **12 → 0**；`common/`、`ai/` 已清空并删除
- 最终：`ui/3 · layout/7 · sections/33 · effects/1 · analytics/1 · carousel/1 · css/4`
- 踩坑（同 S3 的同类问题，第二次印证）：
  1. codemod 第一轮只重写 `@/components/<Name>`，漏了 `@/components/common/<Name>`（28 处）
  2. 移动带 CSS Module 依赖的组件须同步改相对路径（`./css/` 与 `../css/` → `../../css/`）
     → **规范：批量路径迁移前必须枚举所有引用前缀（绝对路径 + 各种相对路径）**
- 验证：`tsc` 0 error；`npm run build` ✓Compiled / ✓53/53

### S5 阶段（同日）：ESLint 可执行化

- 旧状态：`.eslintrc.json` 3 行 + eslint@9（flat config 时代）→ **自定义规则一条没执行**
- 先清零存量违规，再把规则设 `error`：
  - `any` ×9 → LogoItem 接口 / `ComponentType<SVGProps<SVGSVGElement>>` / `CarouselSlide[]` / `window._hmt` 精确类型
  - `!` ×2 → PixelBlast 改显式守卫
  - `import type` ×7 → JSX / Metadata
- **去掉 any 立刻暴露 2 个真 bug**：ecommerce/ssl 传入轮播数组缺 `order`；
  核查发现实现用 `a.order ?? 0`（本就可选），故把 `CarouselSlide.order` 改 `order?: number`
- 新增 `eslint.config.mjs`（FlatCompat 桥接 next 预设）；删除 `.eslintrc.json`
- package.json 显式声明 `@eslint/eslintrc` `@typescript-eslint/*`（原本只是传递依赖）
- 新增 `.github/workflows/ci.yml`
- 验证：tsc 0 error；lint **0 error / 150 warning**；build ✓53/53

### S6 阶段（同日）：husky + lint-staged

- 装 husky@9.1.7 + lint-staged@17.5.1
- **重要：`husky init` 生成的 `.husky/pre-commit` 默认是 `npm test`，而本项目无 test 脚本
  → 照原样用会让每次提交都失败。已改为 `npx lint-staged`。**
- husky 9.1 的 `core.hooksPath` = `.husky/_`；用户钩子在 `.husky/pre-commit`
- 双向验证：含 any 的文件被真实 commit 拦截（husky exit 1，lint-staged 自动回滚）；
  合法文件 `npx lint-staged` exit 0 不误拦。测试文件已清理，暂存区已清空。
- 最终：tsc 0 error；lint 0 error/150 warning；build ✓53/53

### S7 阶段（同日）：SectionHeader + FAQ 合并

- 新建 `components/ui/SectionHeader.tsx`
  - **修正了文档的错误估算**：原估 4 处重复 / −120 行；实测**仅 3 处逐字相同**（都在 sections/ai/），
    净减行数≈0。其余是变体（Faqs 用 rounded-md+text-3xl、CapabilityGrid 无徽标+硬编码色、
    host/page 左对齐 h1 级），刻意不合入以免参数膨胀
  - 教训：**估算重复代码的收益前，先用精确字符串匹配核实真实重复数**，别按"看起来像"估
- `sections/ai/FAQSection.tsx` 204 → 157 行：移动端/PC 端各渲染一遍相同 Disclosure → 合并为一份
  - 合并时发现两份还不一致：桌面端漏 `flex-shrink-0` 和 `leading-tight`（移动端有）
  - `key={index}` → `key={faq.question}`
  - **DOM 减半**（8 份 Disclosure → 4 份）
- 验证：tsc 0 error；lint 0 error/150 warning；build ✓53/53
- ⚠ 待验收：/ai /chat /paper /work 的 FAQ 在**桌面断点(≥1024px)** 的间距/图标/行高

### S10 阶段（同日）：假SVG转WebP优化

- **问题**：4个「假SVG」（实际是内嵌base64位图的容器）占5.5MB，完全可以用WebP替代
- **转换结果**：
  - `saas.svg` 3.49MB → `saas.webp` 63KB (-98.2%)
  - `work.svg` 697KB → `work.webp` 48KB (-93.2%)
  - `lw.svg` 694KB → `lw.webp` 50KB (-92.9%)
  - `ai.svg` 618KB → `ai.webp` 77KB (-87.5%)
  - **节省：约5.4MB**
- **更新引用**（6个文件）：
  - `sections/ai/HotProducts.tsx` (4处)
  - `sections/ai/data/products.ts` (4处)
  - `sections/ai/AiCarousel.tsx` (2处)
  - `app/work/page.tsx` (2处)
  - `app/human/page.tsx` (1处)
  - `app/chat/page.tsx` (1处)
- **删除原图**：saas.svg, work.svg, lw.svg 已删除；ai.svg 待删（沙箱保护未执行）
- **验证**：`tsc` 0 error；**build 未复跑**（.next清理触发沙箱保护，纯资源删除无代码风险）
- product 目录：~9MB → 3.6MB（48个文件）
- 已写入《代码规范与优化方案》§13.15
- **后记**：`ai.svg` 已随 P3 阶段删除；残留的 3 个 `_tmp-*` 转换脚本未清理（见待办）

### S8 阶段（同日）：修复 Leftright.tsx 缺失导入 + 钩子复测

- **缺陷**：`src/components/sections/home/Leftright.tsx:365` 调用 `useDebouncedHover` 但**漏了 import**
  → `tsc` 报 TS2304。根因是 §13.7 目录归位时接入防抖 Hook 忘了加导入。
  同类使用点 `ServiceTabs` / `TwoColumnShowcase` / `AiScene` 都正确引入，仅此文件遗漏。
  **教训：批量迁移后必须完整跑一次 tsc，不能只做局部检查。**
- **修复**：在 react 导入后补 `import { useDebouncedHover } from '@/hooks/useDebouncedHover'`
- **husky 钩子二次复测（双向）**：
  - 反向：含 `any` 的暂存文件 `git commit` → 被拦截，exit 1，HEAD 未变，lint-staged 自动回滚现场
  - 正向：合法类型文件 `npx lint-staged` → 通过 exit 0
  - 测试文件 `src/_hook-test.ts` 已删除，暂存区已清空
- 验证：tsc 0 error；lint 0 error/150 warning；build ✓53/53；First Load JS shared 100 kB
- 已写入《代码规范与优化方案》§13.11

### 更正：上面「未完成」清单已过期

以下已在 S5~S8 完成，**不要再当待办**：ESLint flat config（`eslint.config.mjs` 已生，
`.eslintrc.json` 已删）、husky + lint-staged、CI（`.github/workflows/ci.yml`）、
目录迁移（components 已拆 ui/layout/sections/effects/analytics/carousel/css）、
SectionHeader、FAQ 双份渲染合并。

**当时的待办（2026-09-21 晚全量复核后更新）**：`public/images/unused/`（S9 已删）、
62 个 PNG/JPG → WebP（数字已失真，S9 已按用途分类处理）均已完成；
**仍未做**：`TabbedShowcase` −200、巨型页拆分、framer-motion 收敛（40 文件）、
`lucide-react` 卸载（实测 62 图标/112 处，建议搁置）；
**已收尾**：`src/images/` 22 个零引用文件 5.8 MB 已删、`scripts/_tmp-*` 已删、
`.husky/pre-commit` 已入库（《方案》§13.17）；ecommerce/CMSContent 死 image 字段清理、
`Aicarousel` 查实改名 `AiHeroSection`（《方案》§13.18）。
完整清单见《项目长期记忆》"已知待办"。

### S9 阶段（同日）：无用图片资源清理

- 删 `public/images/unused/`：36 文件 / 12.82 MB（全仓 grep `images/unused` 唯一命中是文档自身
  待办条目 → 代码侧零引用）。`git rm -r` 删除，可回滚
- 再做「残留原图 vs 真在用」三列交叉判定（有无同名 .webp / PNG 是否被引用 / .webp 是否被引用），
  又删 4 个 / 3.46 MB：
  - `images/product/gmlart.png` 2364KB（`gmlart.webp` 已在用，PNG 是残留）
  - `images/superiority-bg.jpg` 516KB（全仓零引用）
  - `images/screenshots/PrimaryFeatures.png` 296KB + `-1.png` 369KB（代码用的是 `-2.webp`）
- **收益**：public 下 PNG/JPG 6.79 MB → 3.33 MB（−51%）；本轮累计释放 16.3 MB
- 验证：tsc 0 error。**build 未复跑**（.next 清理触发沙箱保护 + 清理命令被取消；纯资源删除无代码风险）
- 已写入文档 §13.12

### 重要纠正（原文档数字失真）

- **「PNG/JPG 62 个 / 19.4 MB」是错的**：把 `unused/` 里 26 个算进去了。真实剩余 32 个 / 3.33 MB
- **`scripts/optimize-images.js` 的 targetDirs 已过期**：`aisolution` 目录不存在，
  `carousel`/`background` 早已全 WebP 化；而真有 PNG 的 `solutions`/`reference`/`Logoclouds`/`contact`
  都不在列表。照原样跑 = 空转
- **`scripts/update-image-refs.js` 不扫 `.css`（404 风险）**：只处理 `.tsx/.ts/.js/.jsx`。
  而 `components/css/ProductTraits.module.css:37,42` 用 `url(/images/reference/*.png)` 引了 2 张图
  → 跑转换前必须先补 CSS 支持
- **不能转 WebP 的黑名单**：`favicon.png`（manifest.json + generate-ico.js 依赖）、
  `images/logos/logo.jpg`（seo.config.ts 的 **og:image**，社交爬虫支持不一）、
  `images/contact/*.png`（**是二维码**，有损压缩破坏可扫性）、`images/Logoclouds/*`（文字 logo，仅 180KB）
- **通用教训**：批量格式转换前必须按用途分类（照片/截图/图标/二维码/SEO 社交图），不能按目录一刀切

> 注：S10（假 SVG 转 WebP）记录见本文档上文，此处不重复。

### P2a 阶段（承接会话）：VideoCarousel MP4 迁移

- 4 个 MP4 共 **10.1 MB** 从 `public/images/screenshots/` → `public/videos/`
  - VideoCarousel.mp4 2868KB, VideoCarousel1.mp4 2078KB, VideoCarousel2.mp4 2677KB, VideoCarousel3.mp4 2465KB
- 更新引用：4 处 `videoPath`（VideoCarousel.tsx 默认数据）
- 组件已有精细 preload 逻辑（active:`auto`, inactive:`metadata`），无需改
- **收益**：首屏不预加载视频元数据（~2.1 MB 节省），非活跃 slide 仅下载元数据
- 验证：`tsc` 0 error

### P3 阶段（承接会话）：删 ai.svg

- `public/images/product/ai.svg`（618KB）→ 代码已全切到 `ai.webp`（77KB，-87.5%）
- 验证：`tsc` 0 error

### P1 阶段（承接会话）：死数据字段清理

- **P1-1 已完成**：`ProductsSection.tsx:44` 兜底图 404
  - 删除 `onError` 回调（指向不存在的 `/images/product/placeholder.svg`）
- **P1-2 已完成**：清理 10 处死数据字段
  - work/human/paper/chat 各 4 处 `video: '/videos/*.mp4'`（共 16 处），`/public/videos/` 目录不存在
  - CMSContent.tsx 3 处 `avatar: '/images/avatars/customer-*.jpg'`，`/public/images/avatars/` 目录不存在
  - 同步删除了 4 个文件中的接口定义（Scenario.video, Testimonial.avatar）
  - TypeScript 验证通过，0 error

### 环境注意

- **`npm run dev` 与 `npm run build` 不能同时跑**（共用 `.next`）：并行会报
  `Cannot find module for page: /xxx` 的 ENOENT（2026-09-21 晚实测确认，
  停掉 dev 后构建即恢复正常）。此前文档把构建失败归因于「沙箱 safe-delete 保护」，
  实际主因是这个冲突
- 早期若干次「build 未复跑」的记录里，构建本身是好的，只是当时没跑
- 本节以下为历史会话（Windows + PowerShell + Craft）的环境坑，保留备查：
  - `Remove-Item -Recurse -Force`、`cmd /c rmdir /s /q` 可能触发审批弹窗/被取消
  - PowerShell `Set-Content` / `Get-Content` 不带 `-Encoding` 会被拦截，用文件编辑工具或
    `[System.IO.File]::ReadAllText()`
  - `cmd /c "a; b"` 会把整串交给 cmd；一次只做一件事
  - `node -e` 内联脚本含 `${}` 会被 PowerShell 解析破坏，需写临时 .cjs 文件（用完删）
